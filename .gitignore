# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/

# Artefaux generated output and downloaded sources (never committed)
/out/
/data/

# Local ECG-review rendering — depends on the internal ecg-suite (ecg_io + signalpaper),
# so it is kept out of the self-contained public repo. Regenerate with the workspace
# `/ecg-review-report` command. (The tabular corpus report in reports/ is committed.)
/scripts/reports/render_ecg_review.py
/reports/*_ecg_review.pdf
/reports/*_ecg_review.index.csv

# OS / editor
.DS_Store
